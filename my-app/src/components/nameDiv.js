
import React from "react";
import './nameDiv.css';

const NameDiv = props => (
    <div className="jumbotron">
        <h1>Memory Game!</h1>
        <h2>Click on a puppy pic, but don't click the same one twice!</h2>
    </div>
  );
  
  export default NameDiv;
